# Progress Log

## Learnings
(Patterns discovered during implementation)
- Use `python -m mypy api.py --ignore-missing-imports` for typechecking
- FastAPI and uvicorn are already installed in the venv

---

## Iteration 1 - US-001: Create FastAPI application structure
- What was implemented: Basic FastAPI app with /health endpoint
- Files changed: api.py (created)
- Learnings for future iterations:
  - mypy needed to be installed (`pip install mypy`)
  - Use `--ignore-missing-imports` flag with mypy
  - Test uvicorn by starting server briefly and curling endpoint
---

## Iteration 2 - US-002: Implement pipeline manager with lazy loading
- What was implemented: PipelineManager class with lazy loading, automatic unloading, thread safety
- Files changed: api.py (modified), PRD.md (updated)
- Learnings for future iterations:
  - Pipeline loading code is in demo.py lines 150-166
  - Use `sys.path.insert(0, './hy3dshape')` and `sys.path.insert(0, './hy3dpaint')` for imports
  - Use FastAPI lifespan context manager for startup/shutdown hooks
  - threading.Event.wait() with timeout is cleaner than time.sleep() for interruptible waits
  - Use `Any` type alias for complex ML pipeline types to avoid import issues in type hints
---

## Iteration 3 - US-003: Implement image-to-GLB conversion endpoint
- What was implemented: POST /convert-image-to-3d endpoint with file upload validation, image processing through shape and texture pipelines, and GLB file response
- Files changed: api.py (modified), PRD.md (updated)
- Learnings for future iterations:
  - Image processing logic adapted from demo.py process_image() function (lines 58-143)
  - Use ALLOWED_EXTENSIONS set for validation (png, jpg, jpeg, webp)
  - FileResponse with proper media_type and Content-Disposition header for file downloads
  - When using PIL Image.open(), need to handle type difference between ImageFile and Image.Image
  - Files saved to outputs/ directory with timestamp-based naming
  - Cleanup intermediate files (untextured GLB, OBJ, MTL, textures) after processing
---

## Iteration 4 - US-004: Add permanent file storage
- What was implemented: GET /outputs endpoint to list stored GLB files, GET /outputs/{filename} endpoint to download files
- Files changed: api.py (modified), PRD.md (updated)
- Learnings for future iterations:
  - Use `{filename:path}` in FastAPI route to allow slashes in the filename parameter
  - Always include path traversal protection when serving files from a directory
  - Files are stored in subdirectories like `outputs/{name}_{timestamp}/{name}_textured.glb`
  - The list endpoint returns relative paths from outputs/ directory
---

## Iteration 5 - US-005: Add error handling and logging
- What was implemented: Comprehensive logging and error handling for the API
- Files changed: api.py (modified), PRD.md (updated)
- Learnings for future iterations:
  - Use `logging.basicConfig()` with format string for consistent log output
  - Named logger (e.g., `logging.getLogger("hunyuan3d-api")`) helps filter logs in production
  - Return `JSONResponse` with status_code=500 for conversion errors instead of raising HTTPException
  - Use `traceback.format_exc()` to get full stack trace for error logging
  - Track processing time with `time.time()` before and after operations
  - Log both start and completion/failure of conversion requests with filename and duration
---
